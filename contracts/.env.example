# Deployer key for `forge script script/Deploy.s.sol --rpc-url ark_devnet --broadcast`.
# Devnet only. For anything with real value, use a hardware wallet or `scriipture deploy --browser`.
PRIVATE_KEY=0x
# Set to true to also create a funded demo vault owned by the deployer.
DEMO_VAULT=false
